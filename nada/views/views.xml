<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="nada.list">
      <field name="name">nada list</field>
      <field name="model">nada.nada</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="nada.action_window">
      <field name="name">nada window</field>
      <field name="res_model">nada.nada</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="nada.action_server">
      <field name="name">nada server</field>
      <field name="model_id" ref="model_nada_nada"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="nada" id="nada.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="nada.menu_1" parent="nada.menu_root"/>
    <menuitem name="Menu 2" id="nada.menu_2" parent="nada.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="nada.menu_1_list" parent="nada.menu_1"
              action="nada.action_window"/>
    <menuitem name="Server to list" id="nada" parent="nada.menu_2"
              action="nada.action_server"/>
    -->
  </data>
</odoo>