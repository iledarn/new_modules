<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="connector_nada.list">
      <field name="name">connector_nada list</field>
      <field name="model">connector_nada.connector_nada</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="connector_nada.action_window">
      <field name="name">connector_nada window</field>
      <field name="res_model">connector_nada.connector_nada</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="connector_nada.action_server">
      <field name="name">connector_nada server</field>
      <field name="model_id" ref="model_connector_nada_connector_nada"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="connector_nada" id="connector_nada.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="connector_nada.menu_1" parent="connector_nada.menu_root"/>
    <menuitem name="Menu 2" id="connector_nada.menu_2" parent="connector_nada.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="connector_nada.menu_1_list" parent="connector_nada.menu_1"
              action="connector_nada.action_window"/>
    <menuitem name="Server to list" id="connector_nada" parent="connector_nada.menu_2"
              action="connector_nada.action_server"/>
    -->
  </data>
</odoo>